const styles = (theme) => ({
  root: {
    flexGrow: 1,
    height: '100%',
    background: theme.palette.primary.main,
    padding: '22%',
  },
  subheading: {
    fontSize: 30,
    fontWeight: 400,
    color: 'Black',
  },
});

export default styles;
